- Please expand the Block, that fits your operating system:
- ### Windows
  collapsed:: true
	- Use this command to install git
	  ```shell
	  winget install --id Git.Git -e --source winget
	  # Verity Installation
	  git --version
	  ```
- ### Linux
  collapsed:: true
	- Use this command to install git
	  
	  ```shell
	  sudo apt install git
	  # Verity Installation
	  git --version
	  ```
- ### MacOS
  collapsed:: true
	- Use this command to install git
	  
	  ```shell
	  brew install git
	  # Verity Installation
	  git --version
	  ```